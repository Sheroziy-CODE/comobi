dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven {url "https://jitpack.io" } // Add Jitpack
    }
}
rootProject.name = "COMOBI"
include ':app'
