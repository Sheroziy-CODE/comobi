dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven {url "https://jitpack.io" } // Chat SDK
    }
}
rootProject.name = "COMOBI"
include ':app'
